NAME = school

CC = gcc

SRC = $(wildcard *.c)

OBJ = $(patsubst %.c,%.o,$(wildcard *.c))

RM = rm -f

all: $(OBJ)
	$(CC) $(OBJ) -o $(NAME)

clean:
	$(RM) $(NAME) $(wildcard *~) $(find . -type f -name ".*.sw[klmnop]" -delete)

oclean:
	$(RM) $(OBJ)

fclean: oclean

re: fclean all
